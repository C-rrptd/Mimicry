flask
yt-dlp
# Bundles the JS solver script yt-dlp needs for YouTube's n-challenge, so no
# runtime fetch from GitHub is needed. Keep this updated alongside yt-dlp —
# its version has to track yt-dlp's closely, and `pip install -U yt-dlp`
# alone does not reliably bump this too (see README Troubleshooting).
yt-dlp-ejs
requests
pywebview
mutagen
Pillow
pystray; sys_platform == "win32"
deno
# Spotify's Web API (see fetch_spotify_metadata in app.py) — requires a
# real user login via OAuth, not just an API key; see Settings > Spotify.
spotipy
